✅ INSCRIPTION CONFIRMÉE

Bonjour <%= @user.first_name %>,

<% if @is_child_registration %>
L'inscription de votre enfant <%= @participant_name %> à l'événement <%= @event.title %> a bien été enregistrée.
<% else %>
Votre inscription à l'événement <%= @event.title %> a bien été enregistrée pour <%= @participant_name %>.
<% end %>

📅 DÉTAILS DE L'ÉVÉNEMENT
─────────────────────────

👤 Participant : <%= @participant_name %><%= @is_child_registration ? " (enfant)" : "" %>

📍 Lieu : <%= @event.location_text %>
🕐 Date : <%= l @event.start_at, format: :event_long, locale: :fr %>
⏱️ Durée : <%= @event.duration_min %> minutes
<% if @event.price_cents > 0 %>
💰 Prix : <%= number_to_currency(@event.price_cents / 100.0, unit: '€', separator: ',', delimiter: ' ') %>
<% end %>
<% if @event.route.present? %>
🗺️ Parcours : <%= @event.route.name %>
<% end %>
<% unless @event.unlimited? %>
👥 Participants : <%= @event.active_attendances_count %> / <%= @event.max_participants %>
  <% if @event.remaining_spots > 0 %>
    (<%= pluralize(@event.remaining_spots, 'place disponible') %>)
  <% end %>
<% end %>

<% if @is_initiation %>
─────────────────────────
🎓 INFORMATIONS INITIATION
─────────────────────────
<% if @attendance.free_trial_used? %>
🎁 Essai gratuit utilisé : Vous avez utilisé votre essai gratuit pour cette séance. Pour continuer à participer aux initiations, une adhésion sera requise.
<% end %>
<% if @attendance.equipment_note.present? %>
📦 Matériel demandé : <%= @attendance.equipment_note %>
(Votre demande a été transmise au staff. Vous serez contacté(e) via WhatsApp si nécessaire.)
<% end %>

📍 Lieu : Gymnase Ampère, 74 Rue Anatole France, 38100 Grenoble
⏰ Horaires : Samedi 10h15 - 12h00 (1h45)
🛡️ Sécurité : Casque et protections fortement recommandés
─────────────────────────
<% end %>

Voir l'événement : <%= @is_initiation ? initiation_url(@event) : event_url(@event) %>

─────────────────────────

À bientôt sur les routes de Grenoble ! 🛼

💡 Rappel : Vous pouvez annuler votre inscription à tout moment depuis la page de l'événement ou depuis "Mes sorties".

