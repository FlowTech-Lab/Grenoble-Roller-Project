<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-8">
      <div class="card card-liquid rounded-liquid shadow-liquid">
        <div class="card-body p-4">
          <!-- Header -->
          <div class="text-center mb-4 pb-3 border-bottom">
            <h1 class="h4 mb-2">
              <i class="bi bi-person-plus text-liquid-primary me-2"></i>Adhérer à l'association
            </h1>
            <p class="text-muted small mb-0">Rejoignez Grenoble Roller pour la saison <%= @season %></p>
          </div>

          <!-- Season info -->
          <div class="alert alert-info mb-4">
            <div class="d-flex align-items-center">
              <i class="bi bi-calendar-event me-2"></i>
              <div>
                <strong>Période de validité :</strong><br>
                Du <%= l(@start_date, format: :long) %> au <%= l(@end_date, format: :long) %>
              </div>
            </div>
          </div>

          <!-- Categories -->
          <div class="row g-3">
            <% @categories.each do |key, info| %>
              <div class="col-12 col-md-4">
                <div class="card h-100 <%= 'border-primary' if key == :adult %>">
                  <div class="card-body text-center">
                    <h5 class="card-title"><%= info[:name] %></h5>
                    <p class="text-muted small mb-3"><%= info[:description] %></p>
                    <div class="mb-3">
                      <span class="h4 text-primary">
                        <%= number_to_currency(info[:price_cents] / 100.0, unit: "€", separator: ",", delimiter: " ", format: "%n %u") %>
                      </span>
                    </div>
                    <%= form_with url: memberships_path, method: :post, local: true, data: { turbo: false } do |f| %>
                      <%= f.hidden_field :category, value: key %>
                      <%= f.submit "Choisir", class: "btn btn-primary w-100" %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>

          <!-- Info -->
          <div class="mt-4 pt-3 border-top">
            <p class="text-muted small mb-0">
              <i class="bi bi-info-circle me-1"></i>
              Le paiement sera effectué via HelloAsso. Vous serez redirigé vers leur plateforme sécurisée.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
